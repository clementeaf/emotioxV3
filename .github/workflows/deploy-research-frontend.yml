name: Deploy Research Frontend to S3/CloudFront

on:
  push:
    branches:
      - main
    paths:
      - 'research-frontend/**'
      - '.github/workflows/deploy-research-frontend.yml'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to S3 and CloudFront
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: research-frontend/package-lock.json

      - name: Install dependencies
        working-directory: research-frontend
        run: npm ci

      - name: Build for production
        working-directory: research-frontend
        env:
          VITE_PARTICIPANT_FRONTEND_URL: ${{ secrets.VITE_PARTICIPANT_FRONTEND_URL }}
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to S3
        working-directory: research-frontend
        run: |
          aws s3 sync dist/ s3://${{ secrets.RESEARCH_FRONTEND_S3_BUCKET }} --delete --exclude "runtime-config.json"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.RESEARCH_FRONTEND_CLOUDFRONT_ID }} \
            --paths "/*"
