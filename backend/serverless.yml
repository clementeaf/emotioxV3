service: emotioxv3-backend

provider:
  name: aws
  runtime: nodejs20.x
  region: ${env:AWS_REGION, 'us-east-1'}
  stage: ${env:API_STAGE, 'dev'}
  timeout: 60
  memorySize: 1024
  environment:
    DB_HOST: ${ssm:/emotioxv3/${self:provider.stage}/DB_HOST}
    DB_PORT: ${ssm:/emotioxv3/${self:provider.stage}/DB_PORT}
    DB_NAME: ${ssm:/emotioxv3/${self:provider.stage}/DB_NAME}
    DB_USER: ${ssm:/emotioxv3/${self:provider.stage}/DB_USER}
    DB_PASSWORD: ${ssm:/emotioxv3/${self:provider.stage}/DB_PASSWORD~true}
    DB_SSL: ${ssm:/emotioxv3/${self:provider.stage}/DB_SSL}
    APP_AWS_REGION: ${ssm:/emotioxv3/${self:provider.stage}/APP_AWS_REGION}
    S3_BUCKET_NAME: ${ssm:/emotioxv3/${self:provider.stage}/S3_BUCKET_NAME}
    CORS_ORIGIN: ${ssm:/emotioxv3/${self:provider.stage}/CORS_ORIGIN}
    COGNITO_USER_POOL_ID: ${ssm:/emotioxv3/${self:provider.stage}/COGNITO_USER_POOL_ID}
    COGNITO_CLIENT_ID: ${ssm:/emotioxv3/${self:provider.stage}/COGNITO_CLIENT_ID}
    JWT_SECRET: ${ssm:/emotioxv3/${self:provider.stage}/JWT_SECRET~true}
    JWT_REFRESH_SECRET: ${ssm:/emotioxv3/${self:provider.stage}/JWT_REFRESH_SECRET~true}
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - s3:PutObject
            - s3:GetObject
            - s3:DeleteObject
          Resource:
            - "arn:aws:s3:::${env:S3_BUCKET_NAME}/*"
            - "arn:aws:s3:::emotioxv3-media-*/*"
        - Effect: Allow
          Action:
            - cognito-idp:AdminGetUser
            - cognito-idp:AdminSetUserPassword
            - cognito-idp:AdminUpdateUserAttributes
          Resource: "arn:aws:cognito-idp:${env:AWS_REGION}:*:userpool/*"
        - Effect: Allow
          Action:
            - ec2:CreateNetworkInterface
            - ec2:DescribeNetworkInterfaces
            - ec2:DeleteNetworkInterface
            - ec2:AssignPrivateIpAddresses
            - ec2:UnassignPrivateIpAddresses
          Resource: "*"

functions:
  api:
    handler: dist/handler.handler
    events:
      - http:
          path: /{proxy+}
          method: ANY
          cors:
            origins:
              - http://localhost:12500
              - http://localhost:12600
              - http://localhost:3000
              - http://localhost:5173
              - http://localhost:5174
              - https://research.useremotion.com
              - https://participant.useremotion.com
            headers:
              - Content-Type
              - Authorization
              - Cookie
              - Set-Cookie
              - X-Amz-Date
              - X-Api-Key
              - X-Amz-Security-Token
            allowCredentials: true
      - http:
          path: /
          method: ANY
          cors:
            origins:
              - http://localhost:12500
              - http://localhost:12600
              - http://localhost:3000
              - http://localhost:5173
              - http://localhost:5174
              - https://research.useremotion.com
              - https://participant.useremotion.com
            headers:
              - Content-Type
              - Authorization
              - Cookie
              - Set-Cookie
              - X-Amz-Date
              - X-Api-Key
              - X-Amz-Security-Token
            allowCredentials: true

plugins:
  - serverless-offline

custom: {}
