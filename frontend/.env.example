# 🌐 PUBLIC TESTS URL
# URL base para public-tests (desarrollo local vs producción)
NEXT_PUBLIC_PUBLIC_TESTS_URL=http://localhost:5173

# 🔌 API ENDPOINTS - USAR AWS LAMBDA (NO HAY BACKEND LOCAL)
# URLs de la API backend AWS Lambda
NEXT_PUBLIC_API_URL=https://h68qs1et9j.execute-api.us-east-1.amazonaws.com/dev
NEXT_PUBLIC_WS_URL=wss://b59weq4qqh.execute-api.us-east-1.amazonaws.com/dev

# 🎯 CONFIGURACIÓN DE ENTORNO
# Variables de control de entorno
NEXT_PUBLIC_USE_LOCAL_BACKEND=false
NEXT_PUBLIC_FORCE_AWS=true
NODE_ENV=development

# 📋 INSTRUCCIONES:
# 1. Copia este archivo a .env.local para desarrollo
# 2. Las variables se detectan automáticamente según el entorno
# 3. Frontend local (localhost:3000) → Public-tests local (localhost:5173)
# 4. Frontend desplegado → Public-tests desplegado (CloudFront)
# 5. Backend: SIEMPRE AWS Lambda (no hay backend local)
